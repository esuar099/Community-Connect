## Linux Prereqs

- Git - install through your package manager

```
Check install
$ git --version
```

- Node v16  - install through your package manager

```
Check install
$ node -v
$ npm -v
```

- Yarn
```
Install
$ npm i -g yarn

Check install
$ yarn -v
```
